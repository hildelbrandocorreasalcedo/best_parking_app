import 'package:flutter/material.dart';
import '';

class ConsultaFinacieraDia extends StatelessWidget {
  const ConsultaFinacieraDia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
